[workspace]
members = [
    "firmware",
    "host",
]

resolver = "2"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[patch.crates-io]
embassy-executor = { path = "patches/embassy-executor-0.10.0" }
