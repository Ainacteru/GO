[package]
name = "go"
version = "0.1.0"
edition = "2021"
resolver = "2"

[package.metadata]
chip = "ATSAMD21G18A"

[dependencies]
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = { version = "0.7", optional = true }

atsamd-hal = { version = "0.23.3", default-features = false}

usb-device = { version ="0.3.2", optional = true }
usbd-serial ={ version = "0.2.2", optional = true }

libm = "0.2.16"
defmt = "1.1.0"
portable-atomic = "1.13.1"

embassy-time = { version = "0.5.1", features = ["defmt", "tick-hz-1_000"] }
embassy-time-driver = "0.2.2"
embassy-executor = { version = "0.10.0", features = ["defmt", "executor-thread", "platform-cortex-m"] }

bmp390-rs = { version = "0.4.0", features = ["uom"] }
uom = { version = "0.37.0", features = ["f32", "si"], default-features = false }

critical-section = "1.2.0"
micromath = { version = "2.1.0", features = ["quaternion", "vector"] }
embassy-embedded-hal = "0.6.0"
embassy-sync = "0.8.0"
static_cell = { version = "2.1.0" }
embedded-hal-bus = { version = "0.3.0", features = ["async", "defmt-03"] }
thiserror = { version = "2.0.18", default-features = false }
# heapless = { version = "0.9.3", features = ["defmt"] }

# sd card
embedded-fatfs = { git = "https://github.com/MabezDev/embedded-fatfs", rev = "518528c", default-features = false, features = ["lfn"] }
sdspi = { git = "https://github.com/MabezDev/embedded-fatfs", rev = "518528c" }
block-device-adapters = { git = "https://github.com/MabezDev/embedded-fatfs", rev = "518528c", features = ["defmt"] }
aligned = "0.4.2"
embedded-io-async = "0.7.0"

[features]
default = ["rt", "usb", "async", "dma", "atsamd-hal/samd21g"]
rt = ["dep:cortex-m-rt", "atsamd-hal/samd21g-rt"]
usb = ["atsamd-hal/usb", "dep:usb-device", "dep:usbd-serial",]
async = ["atsamd-hal/async"]
dma = ["atsamd-hal/dma"]

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
